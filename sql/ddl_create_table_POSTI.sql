CREATE TABLE "POSTI" 
   (	"ID_POSTO" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 99999999999 INCREMENT BY 1 START WITH 1 NOT NULL, 
	"CODICE_VOLO" VARCHAR2(50) NOT NULL, 
	"NUMERO_POSTO" NUMBER(3,0) NOT NULL, 
	"STATO" VARCHAR2(20) NOT NULL, 
	 CONSTRAINT "PK_ID_POSTO" PRIMARY KEY ("ID_POSTO")
   ); COMMIT;
